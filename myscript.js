"use strict";

/*console.log("God is good");

let a = parseInt("3.22");
var b = 2;
var c = a - b;

console.log(c);

console.log(typeof c);

console.log(a);

function calAge1(birthyear) {
  return 2025 - birthyear;
}

const ageNew = calAge1(1999);

console.log(ageNew);

const areaOfCircle = function (base, height) {
  return 0.5 * base * height;
};

const areaOfCircleResult = areaOfCircle(5, 7);

console.log(areaOfCircleResult);

const calAge3 = (birthyear) => 2025 - birthyear;

const cutFruitPieces = function (fruit) {
  return fruit * 3;
};

const fruitProcessors = function (apple, oranges) {
  const cutApplePieces = cutFruitPieces(apple);
  const cutOrangesPieces = cutFruitPieces(oranges);

  const juices = `We have ${cutApplePieces} apple and ${cutOrangesPieces} oranges`;
  return juices;
};

console.log(fruitProcessors(50, 10));

const friends = ["xavier", "sunny", "bola", "toxin", "sonia"];

console.log(friends.length);
console.log(friends[friends.length - 2]);

friends[2] = "lemon";
console.log(friends);

const sunnyAge = function (birthyear) {
  return 2025 - birthyear;
};

const ageArray = [2005, 2001, 1992, 1990, 1932];

const newAge = sunnyAge(ageArray[1]);
const newAge2 = sunnyAge(ageArray[ageArray.length - 1]);
const newAge3 = sunnyAge(ageArray[2]);
const newAge4 = sunnyAge(ageArray[3]);

console.log(newAge, newAge2, newAge3, newAge4);

const sunnyObject = {
  firstname: "Sunday",
  lastname: "Yusuf",
  age: 2025 - 2003,
  job: "Graphic Designer",
  friends: ["sunny", "xavier", "Joseph", "Sharon"],
};

console.log(sunnyObject);
console.log(sunnyObject.age);

const interestedIn = prompt(
  "Hello my guy, kindly choose between first name, last name, age, job, friends"
);

console.log(sunnyObject[interestedIn]);

console.log(
  `${sunnyObject.firstname}, has ${sunnyObject.friends} ${jonas.friends.length}`
);
*/
/*
fetch('https://jsonplaceholder.typicode.com/todos/1')
      .then(response => response.json())
      .then(json => console.log(json))

async function fetchTodo() {
  try {
    const response = await fetch(
      "https://jsonplaceholder.typicode.com/todos/1"
    );
    const data = await response.json();

    console.log(data);
  } catch (error) {
    console.log("Error fetching data");
  }
}

const jonas = [
  "john",
  "schemtman",
  2025 - 1999,
  "teacher",
  ["Michael", "Peter", "steven"],
];

for (let i = jonas.length - 1; i >= 0; i--) {
  console.log(i, jonas[i]);
}

for (let exercise = 1; exercise < 4; exercise++) {
  console.log(`------- Starting Exercise ${exercise}`);

  for (let rep = 1; rep < 6; rep++) {
    console.log(`lifting weight repetition🏋️ ${rep}`);
  }
}

const x = 23;
if (x === 23) {
  console.log("God is the greatest");
}
//BUG


const temperatureConversion = function () {
  const temperatureConversion1 = {
    type: "temp",
    unit: "celsius",
    value: Number(prompt("Please input the value for celsius")),
  };

  const newTemperature = temperatureConversion1.value + 270;
  return newTemperature;
};

console.log(temperatureConversion());


const testh1 = document.querySelector("#title");
const paragraph = document.querySelector(".info");
const box1 = document.querySelector("#box");

testh1.innerText = "Hello Javascript";
testh1.style.color = "red";
paragraph.style.color = "green";
box1.style.backgroundColor = "red";

testh1.addEventListener("click", function () {
  alert("You just click this ");
});

*/
